import 'package:flutter/material.dart';

class BuySphereColors {
  // Cores principais da marca
  static const Color primary = Color(0xFF0056B3);      // Azul principal
  static const Color secondary = Color(0xFF28A745);    // Verde secundário
  static const Color accent = Color(0xFFFFC107);       // Amarelo destaque
  static const Color background = Color(0xFFF8F9FA);   // Cinza claro
  static const Color white = Color(0xFFFFFFFF);        // Branco
  static const Color black = Color(0xFF000000);        // Preto
  
  // Cores dos apps
  static const Color buyConnect = Color(0xFF3B82F6);    // Azul
  static const Color buyDrive = Color(0xFF10B981);      // Verde
  static const Color buyBank = Color(0xFF8B5CF6);       // Roxo
  static const Color buyExpress = Color(0xFFF97316);    // Laranja
  static const Color buyMusic = Color(0xFFEC4899);      // Rosa
  static const Color buyVideo = Color(0xFFEF4444);      // Vermelho
  static const Color buyTravel = Color(0xFF06B6D4);     // Ciano
  static const Color buyFood = Color(0xFFF59E0B);       // Amarelo
  static const Color buyFarma = Color(0xFF14B8A6);      // Teal
  static const Color buyShop = Color(0xFF6366F1);       // Índigo
  static const Color buyCourse = Color(0xFF059669);     // Esmeralda
  static const Color buyUniversit = Color(0xFF7C3AED);  // Violeta
  static const Color buyCity = Color(0xFF64748B);       // Slate
  static const Color buyGame = Color(0xFFF43F5E);       // Rose
  static const Color buyMessage = Color(0xFF84CC16);    // Lima
  
  // Cores de sistema
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);
  
  // Cores de texto
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);
}

